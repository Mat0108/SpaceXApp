import 'package:flutter/material.dart';

class AppColors {
  // Exemple de définition de couleurs
  static const Color background = Color.fromARGB(255, 8, 25, 56);
  static const Color backgroundbar = Color.fromARGB(255, 7, 15, 31);
  static const Color text = Colors.white;
  static const Color card = Color.fromARGB(255, 90, 107, 138);
  static const Color select = Color.fromARGB(255, 111, 141, 196);
}
